{% extends 'base.html.twig' %}

{% block content %}
<div class="container">

        <div class="item  col-xs-4 col-lg-4">
            <div class="thumbnail">
                <img class="group list-group-image" src="http://placehold.it/400x250/000/fff" alt="" />
                <div class="caption">
                    <h4 class="group inner list-group-item-heading">
                        {{ estate.adress }}</h4>
                    <p class="group inner list-group-item-text">
                        {{ estate.type }}</p>
                    <div class="row">
                        <div class="col-xs-12 col-md-6">
                            <p class="lead">
                                {{ estate.price }} €</p>
                        </div>
                    </div>
                    <a href="{{ path('editEstate', {'id': estate.id}) }}" class="btn btn-warning m-1">Editar</a>
                    <a href="{{ path('deleteEstate', {'id': estate.id}) }}" class="btn btn-danger m-1" style="background-color: #F8673E;border-color:#F8673E">Eliminar</a>
                </div>
                    </div>
                </div>
            </div>
        </div>

    </div>
</div>
{% endblock %}